"""MCP server for the voice-to-voice product discovery assistant (Person B).

Two tools over stdio: rag.search (private Chroma catalogue) and web.search
(live results via SerpApi). See docs/MCP.md for the tool schemas.
"""

__version__ = "0.1.0"
